# Copy to web/android/keystore.properties (gitignored) and fill in.
# storeFile points at the path INSIDE the build container; deploy/start.sh
# mounts ~/keystrokes there read-only.
storeFile=/keystrokes/fmiw-release.jks
storePassword=CHANGE_ME
keyAlias=CHANGE_ME
keyPassword=CHANGE_ME
